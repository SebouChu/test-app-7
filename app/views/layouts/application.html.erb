<!DOCTYPE html>
<html>
  <head>
    <title><%= content_for(:title) || "Test App 7" %></title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= yield :head %>

    <%= stylesheet_link_tag "application" %>
  </head>

  <body>
    <main class="container">
      <%= yield %>
    </main>
    <%= javascript_include_tag "application" %>
  </body>
</html>
